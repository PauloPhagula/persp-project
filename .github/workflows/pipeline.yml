name: CI

on:
  pull_request:
  push:
    paths-ignore:
      - '**.md'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: purcell/setup-emacs@master
        with:
          version: 28.1
      - uses: actions/checkout@v4
      - name: Run tests
        run: make package-lint
